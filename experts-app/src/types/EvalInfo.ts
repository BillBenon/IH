export type EvalInfo = {
    evalId: string,
    evalText: string,
    hint: string,
    level: number,
    point: number,
    order: number,
    evalTextFeedback: string,
    evalMetricsFeedback: string
}